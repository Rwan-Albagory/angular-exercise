import { BlockSpecialChars } from './block-special-chars';

describe('BlockSpecialChars', () => {
  it('should create an instance', () => {
    const directive = new BlockSpecialChars();
    expect(directive).toBeTruthy();
  });
});
